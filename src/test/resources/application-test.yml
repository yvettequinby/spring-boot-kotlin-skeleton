helloworld:
  jwt-secret: jwt-secret
spring:
  flyway:
    enabled: false
  datasource:
    username: sa
    password: sa
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
management:
  metrics:
    export:
      datadog:
        enabled: false